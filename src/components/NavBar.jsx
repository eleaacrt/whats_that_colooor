const NavBar = ({
    colorValue,
    setColorValue,
    RGBname
}) => {

    return (
        <div className="nav-bar">
            <a href="" className="link-to-pages">
                circle
            </a>
            <a href="" className="link-to-pages">
                names
            </a>
        </div>
    );
};

export default NavBar;

